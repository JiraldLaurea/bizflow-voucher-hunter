# Development Log

## Implementation
- Created Next.js full-stack MVP scaffold.
- Implemented voucher engine with slots, pools, attempts, final voucher issue, candidate release, duplicate prevention, mock SMS, staff redemption, dashboard metrics, and CSV export.
- Updated UI to follow the supplied public and admin mockups.

## Validation
- `npm run typecheck`: passed.
- `npm run lint`: passed.
- `npm test`: passed, 4 tests.
- `npm run build`: passed.
- `GET /api/health`: passed on `http://127.0.0.1:3000`.
- `npm run test:e2e`: attempted, but Playwright workers failed and the process hung without producing a report. The stuck Playwright/Next processes were stopped manually.
- `npm audit --json`: blocked by policy because it would submit dependency inventory to the external npm audit service without explicit user approval.

## Notes
- `npm install` reported 8 vulnerabilities and a Next.js security warning. A targeted dependency security pass is recommended before production use.

## Public Flow Route Split
- Reworked the customer-facing voucher hunt into separate mobile-first pages:
  - `/campaign/[slug]`
  - `/campaign/[slug]/date`
  - `/campaign/[slug]/time`
  - `/campaign/[slug]/hunt`
  - `/campaign/[slug]/results`
  - `/campaign/[slug]/share`
  - `/campaign/[slug]/confirm`
  - `/campaign/[slug]/confirmation`
- Removed the old combined public campaign client.
- Validation after route split:
  - `npm run typecheck`: passed.
  - `npm run lint`: passed.
  - `npm test`: passed, 4 tests.
  - `npm run build`: passed.

## Campaign Landing Refinement
- Refined Step 1 campaign landing to follow the provided mobile mockup more closely.
- Removed the progress indicator from Step 1 only.
- Added `react-icons` icons for campaign tabs, notification, status, rule rows, and bottom navigation.
- Added Inter through `next/font/local` using local `@fontsource/inter` files so builds do not fetch Google Fonts.
- Reworked the landing gift/ticket visual, rule card, CTA, spacing, type scale, and bottom tabs.
- Validation after refinement:
  - `npm run typecheck`: passed.
  - `npm run lint`: passed.
  - `npm test`: passed, 4 tests.
  - `npm run build`: passed after clearing stale `.next` build artifacts.

## Select Date Refinement
- Removed fake phone status details from the public mobile frame.
- Made the Restaurant / Online Shop tabs persist on customer flow pages after Step 1.
- Reworked Step 2 into a closer mobile Select Date screen:
  - back header
  - persistent campaign tabs
  - calendar-style month picker
  - selected date highlight
  - availability rows with green, orange, and sold-out pills
  - real-time update note
- Added additional seeded restaurant dates for richer availability states. Existing local `data/db.json` files may need reset to show the new seed rows.
- Validation after refinement:
  - `npm run typecheck`: passed.
  - `npm run lint`: passed.
  - `npm test`: passed, 4 tests.
  - `npm run build`: passed.
